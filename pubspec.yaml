name: cossmil
description: "Aplicacion para reserva de citas medicas en COSSMIL"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# flutter build apk --release
# flutter build appbundle --release
# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.3+12


environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0 # Llamadas HTTP REST al servidor
  flutter_secure_storage: ^9.0.0 # Almacenamiento seguro del token
  audioplayers: ^6.1.0 # Reproducción de audio (voz en off splash)
  pdf: ^3.11.1 # Generación de PDF
  printing: ^5.13.3 # Impresión/descarga de PDF
  qr_flutter: ^4.1.0 # Render de código QR en el carnet digital
  sensors_plus: ^6.0.1 # Giroscopio para el efecto holográfico del carnet
  path_provider: ^2.1.4 # Directorio de archivos
  open_file: ^3.5.10 # Abrir archivos generados
  intl: ^0.19.0 # Formato de fechas
  local_auth: ^2.3.0
  crypto: ^3.0.3
  animate_do: ^4.2.0
  geolocator: ^14.0.2
  url_launcher: ^6.3.2
  flutter_local_notifications: ^18.0.0
  timezone: ^0.10.0 # Requerido por flutter_local_notifications para zonedSchedule
  shared_preferences: ^2.5.5
  package_info_plus: ^8.0.0 # Leer versión del APK en tiempo de ejecución
  permission_handler: ^11.3.1 # Verificar y solicitar permisos (notificaciones, ubicación)
  local_auth_android: ^1.0.56
  local_auth_darwin: ^1.6.1
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0 # Notificaciones push (FCM) — favoritos usan almacenamiento local
  in_app_update: ^4.2.3 # Actualizaciones flexibles (pequeñas) vía Google Play
  mobile_scanner: ^7.2.0
  workmanager: ^0.9.0+3
  screen_protector: ^1.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4
  # Reloj falso para probar timeouts sin esperar tiempo real
  # (InitialDataOrchestrator: degradación de recursos no críticos).
  fake_async: ^1.3.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/cossmil_logo.png"
  remove_alpha_ios: true
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/cossmil_logo.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/instructor/
    - assets/vof/
    - assets/vof_tutorial/
    - assets/sounds/

  # Roboto empaquetado localmente: en web el motor lo descarga de
  # fonts.gstatic.com; en redes sin internet (LAN interna) esa descarga
  # falla y el texto no se dibuja. Con el asset local no depende del CDN.
  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Light.ttf
          weight: 300
        - asset: assets/fonts/Roboto-Regular.ttf
          weight: 400
        - asset: assets/fonts/Roboto-Italic.ttf
          style: italic
        - asset: assets/fonts/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/Roboto-Bold.ttf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
