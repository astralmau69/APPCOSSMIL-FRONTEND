# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
# En esta máquina `build` es un SYMLINK a ~/.cache/post2-build (el proyecto vive
# en NTFS y compilar ahí es lento). El patrón `/build/` de arriba solo casa
# directorios, así que el symlink se colaba como archivo sin trackear.
/build

# Proyecto de backend aparte que quedó anidado dentro del repo de la app
# (123 MB). No pertenece a este repositorio.
/ventanilla-mau-avancemau-backend/

# Artefactos de herramientas locales (ruflo / claude-flow), no del proyecto.
/.claude-flow/
/.swarm/
/package-lock.json

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json
# Símbolos de des-ofuscación generados por build_release.ps1 (archivar aparte)
/symbols/

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/.vs

# Respaldo de audios originales antes de normalizar loudness
vof_backup_original/

# Agent-specific directories
.agents/
.claude/skills/

tools/sondeo_calificaciones.txt
